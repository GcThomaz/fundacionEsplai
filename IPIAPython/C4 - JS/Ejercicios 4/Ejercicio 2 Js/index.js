/*2. Crear una función que tome como parámetro un array, retornar el
primer elemento del array */

let numbers = [1, 2, 3, 4, 5];

function returnFirstNumber(numbers){    

    return numbers[0];
}
console.log(returnFirstNumber(numbers));